
const SocialIcon = ({children}) => {
    return (
        <div className="flex justify-center items-center w-10 h-10 border-[#272829] border-[1px] rounded-[12px] mr-4 cursor-pointer">
            {children}
        </div>
    )
}

export default SocialIcon